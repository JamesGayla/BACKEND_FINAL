# backend-repo

Starter Backend repository with a minimal FastAPI app.

Quick start:

- Create a virtualenv and activate it
- Install dependencies: `pip install -r requirements.txt`
- Run: `uvicorn main:app --reload --port 8001`
